import { HistoriqueFormateur } from './historique-formateur';

describe('HistoriqueFormateur', () => {
  it('should create an instance', () => {
    expect(new HistoriqueFormateur()).toBeTruthy();
  });
});
